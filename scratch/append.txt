
/* ==========================================
   響應式設計 (RWD) 佈局最佳化
   ========================================== */

/* 平板版佈局 (Tablet: 768px ~ 1023px) */
@media (min-width: 768px) and (max-width: 1023px) {
  .app-container { flex-direction: column; }
  
  .sidebar {
    width: 100%; height: auto; padding: 20px; position: relative;
    border-right: none; border-bottom: 1px solid var(--panel-border); gap: 20px;
  }
  
  .nav-menu { flex-direction: row; overflow-x: auto; gap: 8px; padding-bottom: 4px; scrollbar-width: none; }
  .nav-menu::-webkit-scrollbar { display: none; }
  
  .nav-item { white-space: nowrap; padding: 10px 16px; font-size: 0.9rem; }
  .nav-item.active { box-shadow: 0 2px 0 var(--accent-color); background: transparent; }
  .nav-item.active::after { display: none; }

  /* 縮小兩側邊距 */
  .main-content { padding: 24px 20px !important; }
  
  /* 確保 KPI 與圓餅圖不換行或適度縮小 */
  .kpi-container { grid-template-columns: repeat(2, 1fr) !important; gap: 16px; }
  .kpi-card { padding: 20px !important; }
  
  .charts-carousel { display: flex; overflow-x: auto; scroll-snap-type: x mandatory; gap: 12px; }
  .carousel-item { flex: 0 0 100%; scroll-snap-align: center; padding: 16px; }
}

/* 手機版佈局 (Mobile: 767px 以下) */
@media (max-width: 767px) {
  .app-container { flex-direction: column; }
  
  .sidebar {
    width: 100%; height: auto; padding: 16px; position: relative;
    border-right: none; border-bottom: 1px solid var(--panel-border); gap: 16px;
  }
  
  .nav-menu { flex-direction: row; overflow-x: auto; gap: 8px; padding-bottom: 4px; scrollbar-width: none; }
  .nav-menu::-webkit-scrollbar { display: none; }
  
  .nav-item { white-space: nowrap; padding: 8px 12px; font-size: 0.85rem; }
  .nav-item.active { box-shadow: 0 2px 0 var(--accent-color); background: transparent; }
  .nav-item.active::after { display: none; }

  /* 極小化邊距 */
  .main-content { padding: 16px 12px !important; }
  
  /* 改為單欄垂直排列 */
  .kpi-container { 
    grid-template-columns: 1fr !important; 
    display: flex !important;
    flex-direction: column !important;
    gap: 16px; 
  }
  
  .kpi-card { 
    padding: 20px !important; 
    align-items: flex-start !important; 
    text-align: left !important;
  }
  .kpi-card #totalProfit { justify-content: flex-start !important; }

  /* 圓餅圖垂直排列 */
  .glass-panel[style*="relative"] {
    display: flex !important;
    flex-direction: column !important;
    gap: 24px;
  }
  .carousel-item { flex: none !important; width: 100% !important; padding: 0 !important; }
  .carousel-hint { display: none !important; }

  /* 縮小表格字體與間距，並啟用橫向捲動 */
  .assets-table {
    font-size: 14px;
    display: block;
    overflow-x: auto;
    white-space: nowrap;
    -webkit-overflow-scrolling: touch;
    width: 100%;
  }
  .assets-table th, .assets-table td {
    padding: 8px !important;
  }
  
  /* 其他區塊縮小 Padding */
  .glass-panel { padding: 16px !important; }
  header { margin-bottom: 24px !important; flex-direction: column; align-items: flex-start; gap: 12px; }
}
